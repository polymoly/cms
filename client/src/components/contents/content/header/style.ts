import { createUseStyles } from "react-jss";

export default createUseStyles({
  header: {
    width: "100%",
    height: "100%",
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    padding: [0, 16],
  },
});
